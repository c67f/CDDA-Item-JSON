# CDDA-Item-JSON
Python code to combine all JSON files from CDDA into one and output them as a CSV file, and then a modified version of that file with damage columns.

Downloading and viewing the .csv file in google sheets (or similar) should let you view and sort the damage columns.
